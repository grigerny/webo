<div class="container">
	<div class="main">
		<% if @q.result.present? %>
			<%= render "search" %>
			<% elsif %>
			<%= @q.result == nil %>
			<%= render "add-once" %>
			<% else %>
			<%= render 'form' %>
			<% end %>
	</div>
</div>